(ns main
  (:require ["package:flutter/material.dart" :as m]
            [audio-app.app-root :as app-root]
            [audio-app.bootstrap :as bootstrap]))

(defn main []
  (m/WidgetsFlutterBinding.ensureInitialized)
  (bootstrap/init-app!)
  (m/runApp (app-root/main-app)))
